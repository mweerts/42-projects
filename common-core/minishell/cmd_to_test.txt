ls ||| test
echo >| test.txt
echo abc > | test.txt
echo abc | > test.txt
ls |
exit > test.txt
echo >< test.txt
ls| + ctrl-D
ls | exit
export TEST=~
echo "test" | > test.txt
expr 2 + 2 <>test1.txt
oefwfe > ||
SHLVL
env ls
env test
exit display "exit" when exiting
export TEST="ls -la"
echo "'aa""aa'"
echo "'aa" "aa'"
"ls akdad"  ' kdpoakdkda'
./t
export test=-la
ls (wc -l)
ls srcs/*
echo """"""''""
echo 123 | wc -c | sleep 1 | echo 568 | cat leaks.supp | exit3 | exit 2 | echo $? | wc -l
ls &&
cat << eof > test // huge leaks, probably a data_free forgotten somewhere
/bin/ls // ok
ls test "*"
$test $test (avec test="ls -la") boucle infini // fixed
printf("split\n");
ctrl-c in extend prompt
ls && (ls fdgfdgd || (echo 1 && echo 2) || ls)

// found with tester
cd $PWD // crash
echo <"test" "bonjour       42"
echo ls > "test1" // i remove the quotes after filename is created which in this case is problematic, also true for expansion i think
$EMPTY echo hi // crash