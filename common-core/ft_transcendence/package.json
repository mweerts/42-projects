{
  "name": "ft-transcendence-monorepo",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start"
  },
  "dependencies": {
    "fastify": "^5.6.1"
  }
}
