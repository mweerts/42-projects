services:
  nginx:
    container_name: nginx
    image: nginx:1.27-alpine
    ports:
      - "8080:80" # For development maybe remove this at the end
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/certs:/etc/nginx/certs:ro
    depends_on:
      - backend
      - frontend
  backend:
    container_name: backend
    build:
      context: ..
      dockerfile: services/backend/Dockerfile
      target: dev
    working_dir: /app/services/backend
    ports:
      - "4983:4983" # for db studio
    environment:
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3000
      - DB_FILE_NAME=${DB_FILE_NAME:-file:./database/app.db}
    volumes:
      - ../services/backend:/app/services/backend
  frontend:
    container_name: frontend
    build:
      context: ..
      dockerfile: services/frontend/Dockerfile
      target: dev
    environment:
      - NODE_ENV=${NODE_ENV:-development}
    expose:
      - "5173" # Vite dev server
    volumes:
      - ../services/frontend/src:/app/services/frontend/src
      - ../services/frontend/index.html:/app/services/frontend/index.html:ro
      - ../services/frontend/vite.config.ts:/app/services/frontend/vite.config.ts:ro
